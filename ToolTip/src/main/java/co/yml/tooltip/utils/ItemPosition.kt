package co.yml.tooltip.utils

/** enum class ItemPosition is created to handle animation state.
 * */
enum class ItemPosition {
    Start, Finish
}

/** enum class AbsoluteOffset is created to handle absoluteOffset(X for horizontal,Y for vertical) according to gravity.
 * */
enum class AbsoluteOffset {
    X, Y
}